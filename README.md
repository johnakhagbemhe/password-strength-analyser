# password-strength-analyser
A privacy-first, browser-based password security tool. Analyses password strength, checks for known data breaches via the HaveIBeenPwned API using k-anonymity, and generates cryptographically secure passwords — all client-side with no data sent to a server.
